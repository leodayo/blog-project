[workspace]

members = [
    "blog-proto",
    "blog-server",
    "blog-client",
    "blog-cli",
    "blog-wasm",
]
resolver = "3"

[workspace.dependencies]
blog-proto = { path = "blog-proto" }
tonic = "0.14"
